# Runtime data

Do not store live pharmacy data in a public copy of this application repository.

The recommended design uses a separate **private** `neochrono-data` repository. The First-time database setup screen creates the JSON files there automatically.
